jobs:
  lintdockerfile:
    docker:
      - image: circleci/docker@2.2.0

    steps:
      - checkout
      - docker/dockerlint:
          dockerfile: ./Dockerfile
          treat-warnings-as-errors: true

workflows:
  sample:
    lint-dockerfile:
      - lintdockerfile
    # test-app:
      # - testapp
    # build-app-karsajobs:
      # - buildappkarsajobs